import React from 'react';

const Form = () => (
  <form>
    <input placeholder="Book title" aria-label="Title" />
    <input placeholder="Author" aria-label="Author" />
    <button type="submit">ADD BOOK</button>
  </form>
);

export default Form;
